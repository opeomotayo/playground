apiVersion: v1
kind: Pod
metadata:
  name: maven-pod
spec:
  containers:
    - name: maven-container
      image: maven:alpine
      imagePullPolicy: Always 
      resources:
        requests:
          memory: "300Mi"
          cpu: "100m"
        limits:
          memory: "500Mi"
          cpu: "300m"
      tty: true
      command:
        - cat
